import classnames from "classname";

export default function TitleSection({ children }) {
  <h2
    className={classnames("text-3xl font-bold font-sans text-lightblue-main")}
  >
    Anforcom
  </h2>;
}